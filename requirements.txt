# 苔藓识别系统 - 运行依赖（Python 3.11）
#
# 【产线部署 · 推荐】用 ONNX 推理，只需下面这些包，无需 torch/timm（部署瘦身）：
#   pip install -r requirements.txt
#   模型用 .onnx：python converter/export_onnx.py models/xxx.pth
#   config.json 的 model_settings.current_model_name 改成 xxx.onnx
#
# 【开发 / 训练 / 导出】额外需要 torch/torchvision/timm，导出还需 onnx：
#   pip install torch==2.9.1+cu128 torchvision==0.24.1+cu128 --index-url https://download.pytorch.org/whl/cu128
#   pip install timm onnx   # onnx 仅 .pth→.onnx 导出时需要

PySide6==6.9.3
Pillow==12.0.0
numpy==2.3.5
onnxruntime==1.28.0
